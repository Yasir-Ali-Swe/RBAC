services:
  postgres:
    image: postgres:18-alpine
    container_name: rbac-postgres

    restart: unless-stopped

    environment:
      POSTGRES_USER: RBAC
      POSTGRES_PASSWORD: RBAC
      POSTGRES_DB: Rbac

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d RBAC"]
      interval: 5s
      timeout: 5s
      retries: 10
      
  redis:
    image: redis:8-alpine
    container_name: rbac-redis

    restart: unless-stopped

    ports:
      - "6379:6379"

    volumes:
      - redis_data:/data

    command: redis-server --appendonly yes

    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  postgres_data:
  redis_data: